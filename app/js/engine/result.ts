module Miner {
  export enum Result {
    SUCCESS,
    INSUFFICIENT_FUNDS,
    OUT_OF_BOUNDS,
    INVALID_TERRAIN,
    TILE_FILLED,
    NOT_ADJACENT,
    INSUFFICIENT_ORE,
    TOO_MANY_INTERVIEWS,
    ALREADY_HIRED_TODAY,
    GAME_OVER
  }
}
